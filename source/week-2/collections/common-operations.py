# importing array
from array import array

# decalare and assign arrays 
scores = array('d')

# adding items to an array
scores.append(97)
scores.append(98)
print(scores)
names = ['Christopher', 'Susan']
print(len(names)) # Get the number of items
names.insert(0, 'Bill') # Insert before index
print(names)

names = ['Christopher', 'Susan']
print(len(names)) # Get the number of items
names.insert(0, 'Bill') # Insert before index
print(names)
